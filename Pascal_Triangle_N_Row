class Main{
    public void NthRow(int n){
        int prev = 1;
        System.out.println(prev);

        for(int i = 0; i < n; i++){
            int curr = prev * (n - i + 1)/i;
            System.out.println(curr);
            prev = curr;
        }
    }
}